// converts age in years to days and displays both values
#include <stdio.h>

int main(void)
{
	int years, days;

	years = 33;
	days = 33 * 365;
	printf("%d years converted in days are %d days.\n", years, days);

	return 0;
}
